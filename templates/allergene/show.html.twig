{% extends 'base.html.twig' %}

{% block title %}Allergene{% endblock %}

{% block body %}
<div class="bg-white flex justify-center items-center max-h-screen m-20">
    <div class="w-full flex-col justify-center bg-white rounded-lg shadow-lg p-4 mb-6">
        <div class="sm:mx-auto sm:w-full sm:max-w-sm">
            <h2 class="mt-10 text-center text-2xl font-bold leading-9 tracking-tight text-gray-900">Gestion des Allergènes</h2>
        </div>

        <div class="flex justify-center">
            <table class="table">
                <tbody>
         
            <tr>
                <th>Nom :</th>
                <td><p class="border-2 border-red-500 rounded-lg text-center shadow-lg text-base text-red-600 w-fit m-2 p-1" >{{ allergene.nom }}</p></td>
            </tr>
            </tbody>
            </table>
        </div>

   <div class="flex justify-center flex-col mt-4">
  <div class="flex justify-center mt-4">
    <a href="{{ path('app_allergene_index') }}">
      <button class="w-auto bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded-md">Retour</button>
    </a>

    <a href="{{ path('app_allergene_edit', {'id': allergene.id}) }}">
      <button class="btn w-20 bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded-md">Éditer</button>
    </a>
  </div>

  <div class="flex justify-center mt-4">
    {{ include('allergene/_delete_form.html.twig') }}
  </div>
</div></div>
</div>
{% endblock %}
