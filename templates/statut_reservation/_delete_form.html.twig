{%if is_granted ('ROLE_ADMIN')%}
<form method="post" action="{{ path('app_statut_reservation_delete', {'id': statut_reservation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ statut_reservation.id) }}">
<button class="btn w-20 bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 ml-2 rounded-md m-2">editer</button>
</form>
{% endif %}